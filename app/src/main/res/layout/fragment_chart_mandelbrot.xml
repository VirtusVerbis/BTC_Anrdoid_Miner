<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageView
        android:id="@+id/mandelbrot_image"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:adjustViewBounds="true"
        android:contentDescription="@string/mandelbrot_chart_image_a11y"
        android:scaleType="centerInside" />

    <TextView
        android:id="@+id/mandelbrot_placeholder"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center"
        android:padding="12dp"
        android:text="@string/mandelbrot_chart_placeholder"
        android:textColor="@color/chart_axis_legend"
        android:textSize="12sp" />

    <TextView
        android:id="@+id/mandelbrot_caption"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_marginEnd="6dp"
        android:layout_marginBottom="4dp"
        android:text=""
        android:textColor="@color/chart_axis_legend"
        android:textSize="11sp"
        tools:text="Mandelbrot" />

</FrameLayout>
